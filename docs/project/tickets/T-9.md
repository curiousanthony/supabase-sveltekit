---
id: T-9
title: Document lifecycle states
status: backlog
priority: P1
type: feature
estimate: medium
chunk: 2
blocked_by: []
plan: null
decision: "2026-04-09-chunk2-document-lifecycle-ux"
created: 2026-04-08
assignee: null
artifacts: []
---
Implement per-type lifecycle states with ASCII status keys. Devis: genere→envoye→accepte|refuse|expire. Convention/Ordre de mission: genere→envoye→signe→archive. Feuille d'émargement: genere→signatures_en_cours→signe→archive. Informational (convocation/certificat): genere→envoye→archive. Terminal states: `annule` (cancelled formation) and `remplace` (superseded by regeneration). No `brouillon` — documents start as `genere`.

## acceptance
- Each document type has its own set of valid lifecycle states with ASCII keys
- `annule` terminal status transitions all in-flight documents when formation is cancelled
- `remplace` terminal status preserves audit trail on regeneration
- Default status is `genere` (not `draft`)
- Automatic transitions fire from system events (email sent → envoye, signature → signe)
- Manual transitions available for devis accept/refuse and manual send marking
- Status stored as text with app-level validation in `document-lifecycle.ts` service

## log
- 2026-04-09 project-manager: Updated from chunk2 brainstorm decisions
