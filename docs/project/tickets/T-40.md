---
id: T-40
title: Postmark template — questionnaire satisfaction formateur (fin de formation)
status: backlog
priority: P2
type: feature
estimate: medium
chunk: 2
blocked_by: []
plan: null
decision: null
created: 2026-04-10
assignee: null
artifacts: []
---

Add a new Postmark **Standard** email template (French copy, relevant merge variables) inviting educators to complete a **« Questionnaire satisfaction formateur »** at the **end of a formation’s lifecycle** (educator-side satisfaction). Implementation must follow the existing pattern in `scripts/setup-postmark-templates.ts`: layout `mentore-manager`, upsert by **Alias**, and the same variable conventions as sibling formation/lifecycle templates. Related product direction: chunk 2 / document lifecycle (see `docs/decisions/2026-04-09-chunk2-document-lifecycle-ux.md` for UX context).

## Critical process (not skippable)

When this ticket is picked up with `assignee: agent`, the implementing workflow **must** include **brainstorming with the human product owner** before any Postmark or script changes:

1. The agent asks clarifying questions (content, tone, timing/triggers, merge variables, legal or Qualiopi wording if applicable).
2. From that conversation, the agent **writes a short plan** or updates ticket **artifacts** with agreed assumptions.

**Do not** treat Phase 1 as skippable by pre-filling `decision` on this ticket. Keep `decision: null` and `plan: null` until **after** that collaboration; then record the outcome (e.g. `plan:` path and/or `decision:` doc, or append to `artifacts`).

## acceptance

- **PO collaboration gate:** Before changing Postmark or `scripts/setup-postmark-templates.ts`, there is documented Q&A with the product owner and a written plan or decision snippet (ticket `plan`/`decision`/`artifacts`) capturing agreed copy, variables, and send-trigger assumptions.
- A new Postmark template exists as a **Standard** template using the shared `mentore-manager` layout; **alias** and **template name** are documented in this ticket body (update this section when implemented).
- French **HTML** and **text** bodies are professional and appropriate for end-of-formation educator outreach.
- Merge variables match project conventions (inspect sibling templates in `setup-postmark-templates.ts` for patterns such as `workspaceName`, formation/session links, etc.) and are sufficient for the agreed trigger.
- `scripts/setup-postmark-templates.ts` is updated so `bun run scripts/setup-postmark-templates.ts` creates or updates this template **idempotently** (upsert by alias, same pattern as existing entries).

### Postmark identifiers (fill in when implemented)

- **Alias:** *TBD*
- **Template name:** *TBD*

## log

- 2026-04-10 project-manager: Ticket created (T-40).