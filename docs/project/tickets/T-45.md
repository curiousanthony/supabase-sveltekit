---
id: T-45
title: Playwright E2E — preflight server-bypass + B2C rétractation scenarios
status: backlog
priority: P2
type: tech-debt
estimate: small
chunk: 2
blocked_by: []
plan: null
decision: null
created: 2026-04-20
assignee: null
artifacts: ["docs/team-artifacts/qa/2026-04-14-T-13-preflight-validation.md"]
---
Two T-13 scenarios were marked SKIP during QA because they require either DevTools network interception or a specific seeded formation: (1) POSTing directly to `generateDocument` and `generateQuestDocument` actions with missing data must be rejected by the server preflight, and (2) a B2C formation with `dateDebut` within 10 days must surface the `retractation_delai` warn item, and acknowledging it must insert into `formation_audit_log`. Add Playwright tests covering both, seeding fixtures via Supabase admin/SQL.

**Orchestrator hint**: `type: tech-debt` + `estimate: small` → recipe `0 → 4 → 5 → 7b`. No brainstorm needed — the spec already lives in the preflight rules and the audit log architecture artifact. Implementer can write the tests directly. Pair with the new qa-tester seeding rules (`.cursor/agents/qa-tester.md`).

## acceptance

- Playwright test: POST to `/formations/[id]/documents?/generateDocument` with missing client returns 4xx and the audit log is unchanged
- Playwright test: POST to `/formations/[id]/suivi?/generateQuestDocument` with missing client returns 4xx
- Playwright test: B2C formation seeded with `dateDebut` 8 days ahead → preflight shows `retractation_delai` warn → acknowledging proceeds → `formation_audit_log` row inserted with `actionType = 'document_generation_warnings_overridden'`
- Tests run green in CI

## log
