---
id: T-42
title: Preflight summary separates blocages from prérequis
status: backlog
priority: P3
type: bug
estimate: small
chunk: 2
blocked_by: []
plan: null
decision: "2026-04-09-chunk2-document-lifecycle-ux"
created: 2026-04-20
assignee: null
artifacts: ["docs/team-artifacts/qa/2026-04-14-T-13-preflight-validation.md"]
---

The preflight dialog currently writes `Génération impossible : N blocage(s).` where `N` lumps together data blocks (fix on Fiche/Apprenants) and prerequisites (fix in Suivi/devis). Marie cannot tell what kind of fix is needed from the count. Split into `N donnée(s) manquante(s), M prérequis` (or equivalent French wording) and update the live region. Touch `evaluatePreflight` to expose the two counts separately. Source: T-13 QA finding M2.

**Orchestrator hint**: `type: bug` + `estimate: small` → recipe `0 → 4 → 5 → 7b`. The wording change is small UX copy; no Design Council needed (decision doc covers context).

## acceptance

- Header reads e.g. "Génération impossible : 1 donnée manquante, 1 prérequis"
- Live region (`aria-live="polite"`) reflects both counts
- Existing 42 preflight tests still pass; add 1 test asserting the two counts on a mixed scenario

## log
