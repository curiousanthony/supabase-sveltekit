---
id: T-43
title: Preflight dialog header shows "X sur Y éléments prêts"
status: backlog
priority: P3
type: feature
estimate: small
chunk: 2
blocked_by: []
plan: null
decision: "2026-04-09-chunk2-document-lifecycle-ux"
created: 2026-04-20
assignee: null
artifacts: ["docs/team-artifacts/qa/2026-04-14-T-13-preflight-validation.md", "docs/team-artifacts/design/2026-04-13-T-13-preflight-validation-ux.md"]
---
The original UX review for T-13 specified a progress counter `Prêt à générer — X sur Y éléments` in the dialog header (Y = applicable rows for the selected document type only). Counter was not implemented in T-13. Source: T-13 QA finding L1.

**Orchestrator hint**: `type: feature` but `decision` and UX artifact already cover it — recipe collapses to `0 → 4 → 5 → 6 → 7b` (Phase 1 Design Council can be skipped because the UX review already prescribes the wording and placement). A `plan` is *not* set, so Phase 1b (plan validation) should still produce a small plan or the ux-designer can write one quickly.

## acceptance

- Dialog header shows "Prêt à générer — X sur Y éléments" reactive to current selection (contact, formateur, séance)
- Y counts only applicable rows for the selected document type
- X counts rows currently in the OK state (not block, not warn, not prerequisite)
- a11y: counter is part of the dialog title accessible name

## log
