---
id: T-55
title: "Schema + Fiche fields: codeRs, codeCpfFiche, niveauQualification, certificateur, dateEnregistrementRncp"
status: done
priority: P1
type: feature
estimate: small
chunk: fiche-finances-redesign
blocked_by: [T-63]
plan: docs/plans/2026-04-21-T-formation-fiche-finances-redesign.plan.md
decision: null
created: 2026-04-21
assignee: agent
artifacts:
  - docs/team-artifacts/analysis/2026-04-21-formation-fiche-audit.md
  - docs/team-artifacts/design/2026-04-21-formation-fiche-finances-ux-review.md
---

Add the missing référencement fields to the schema so the Fiche Référencement card (T-54) can render them. Code RNCP alone covers only a minority of certifying OFs — Code RS (Répertoire Spécifique: TOSA, PIX, SST, PCIE...) is mandatory for many common certifications, and Code CPF (numéro de fiche Mon Compte Formation) is required for any CPF-eligible formation.

## acceptance

- Schema additions to `formations` table: `code_rs` text, `code_cpf_fiche` text, `niveau_qualification` integer (CHECK between 1 and 8), `certificateur` text, `date_enregistrement_rncp` date
- All five fields exposed via the Fiche Référencement card (built in T-54)
- `allowedFields` in `src/routes/(app)/formations/[id]/fiche/+page.server.ts` extended to include the five new fields
- Placeholder copy hints at the expected format (e.g. `RS5832`, `285568`)
- Migration includes a down step that drops the columns

## log

- 2026-04-21 implementer: added 5 columns to `formations` (`code_rs`, `code_cpf_fiche`, `niveau_qualification`, `certificateur`, `date_enregistrement_rncp`) — all nullable. Drizzle schema mirrors them as `codeRs`, `codeCpfFiche`, `niveauQualification`, `certificateur`, `dateEnregistrementRncp`.
- 2026-04-21 implementer: migration `20260421140000_t55_referencement_fields.sql` is idempotent (`ADD COLUMN IF NOT EXISTS`) and adds the CHECK constraint `niveau_qualification IS NULL OR (niveau_qualification BETWEEN 1 AND 8)` via a `pg_constraint` guard so reruns are no-ops.
- 2026-04-21 implementer: `+page.server.ts` `allowedFields` extended with the 5 new keys; `niveauQualification` parses to int and enforces the 1-8 bound server-side (rejects out-of-range silently → null) so a client crafting `0` or `9` cannot bypass the DB check; `dateEnregistrementRncp` joins the existing date passthrough branch.
- 2026-04-21 implementer: UI rendering of these fields lives with T-54 (Référencement collapsible card) — this ticket only ships the data plane. Down migration not written here; T-54 work / a follow-up will define the rollback story for the whole référencement section.
- 2026-04-21 implementer: `bun run check` shows the same 72/32 baseline — no new type errors. Runtime import verified the 5 new columns are exposed under their snake_case PG names.
