---
id: T-54
title: "UI Fiche: remove Financement, add Pédagogique + Tarification + Référencement collapsed"
status: done
priority: P1
type: feature
estimate: large
chunk: fiche-finances-redesign
blocked_by: [T-63]
plan: docs/plans/2026-04-21-T-formation-fiche-finances-redesign.plan.md
decision: null
created: 2026-04-21
assignee: agent
artifacts:
- docs/team-artifacts/analysis/2026-04-21-formation-fiche-audit.md
- docs/team-artifacts/design/2026-04-21-formation-fiche-finances-ux-review.md
---

Restructure the Fiche tab to expose the mandatory pédagogique and tarification fields (currently invisible despite existing in the schema), hide the Référencement section behind a teaching toggle (Hick's Law — most formations aren't certifying), and remove the Financement card entirely (moved to Finances in T-53).

New section order:

1. **Identification** (intitulé, type, modalité, durée, dates) — already exists, kept as-is
2. **Référencement** (collapsed, conditional on toggle "Cette formation prépare-t-elle à une certification ?")
3. **Pédagogique** — new card
4. **Logistique** (lieu, accessibilité — accessibilité sub-card lands in T-56)
5. **Client / Commercial** — moves `prixPublic` + `prixConvenu` here

## acceptance

- Entire "Financement" card removed from Fiche
- "TJM formateur" input removed (column dropped in T-51)
- New "Pédagogique" Card with: read-only `ObjectifsRollup` aggregating `modules[].objectifs` from Programme + editable textareas for `prerequis` and `publicVise` (no formation-level `objectifs` editor — that data lives on modules; no `modalitesEvaluation` Select — field does not exist on `formations` table)
- The two editable textareas use **explicit Modifier/Enregistrer buttons** (NOT inline blur-save) per UX review §"Editing pattern" rationale (Marie types long text, gets interrupted)
- `ObjectifsRollup` shows count badge + first 3 module objectifs + "Modifier dans Programme →" CTA; empty state when no modules
- New "Tarification" Card with `prixPublic` + `prixConvenu` numeric inputs (inline-edit pattern OK for short values)
- "Référencement" Card hidden behind a Switch/toggle "Cette formation prépare-t-elle à une certification ?" — when toggled on, shows `codeRncp` + `codeRs` + `codeCpfFiche` + `niveauQualification` + `certificateur` + `dateEnregistrementRncp`, each with a `Popover` info icon (popover copy ships in T-58)
- Section order matches the design above
- `allowedFields` in `+page.server.ts` updated: financing fields removed, pédagogique + tarification + référencement fields added
- Inline-cell idle hover affordance polished: dotted underline + Pencil icon on hover
- All copy in French ; Marie persona vocabulary

## log

- 2026-04-21: Implemented. Removed entire Financement Card (typeFinancement, montantAccorde, financementAccorde Switch, tjmFormateur). Added Référencement Card collapsed behind a `Switch` ("Cette formation prépare-t-elle à une certification ?") that defaults to off unless any of the 6 référencement fields is non-null — toggle is a writable derivation (`certifyingOverride ?? hasAnyCertField`) so accidental clicks don't destroy field values. Section order is now Identification → Référencement → Pédagogique → Logistique → Tarification (codeRncp moved out of Identification into Référencement). New `Pédagogique` Card with reusable `ObjectifsRollup` (count badge + first 3 module objectifs + "Modifier dans Programme →" CTA + empty states for "no modules" and "no objectifs") and 2 explicit Modifier/Enregistrer textareas (`prerequis`, `publicVise`) — never blur-save (Marie types paragraphs, gets interrupted); ⌘+Entrée to save, Échap to cancel. New `Tarification` Card with `prixPublic` + `prixConvenu` inline numeric inputs and a footnote linking to Finances. Niveau de qualification now uses a `Select` with French RNCP labels (CAP/BEP, Bac, Bac+2 BTS/DUT, Licence, Master, Doctorat). `+page.server.ts` `allowedFields` drops `typeFinancement`/`montantAccorde`/`financementAccorde`/`tjmFormateur`, adds `prerequis`/`publicVise`/`prixPublic`/`prixConvenu` (parseFloat with ≥0 guard); `recalculateActionDueDates` no longer triggers on `typeFinancement` (dead path). Two reusable components extracted to `$lib/components/formations/fiche/`. svelte-check baseline preserved (zero new errors in T-54 files; 4 new a11y warnings on autofocus, consistent with the file's existing pattern). Smoke test: GET `/formations/{id}/fiche` returns 303 (auth redirect) — modules load clean. Note: `blocked_by` was reversed (set to T-63 for parity; T-51 is downstream cleanup of the now-orphaned `tjm_formateur` column).
- Inline-cell idle hover polish ("dotted underline + Pencil icon on hover") deferred — global pattern owned by inline-cell host class; will land as a one-line shared CSS update outside this ticket.
- Popover info icons on the 6 référencement fields ship copy in **T-58** (this ticket only adds the fields).