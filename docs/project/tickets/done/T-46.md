---

id: T-46
title: "Security: bind formation_audit_log inserts to auth.uid()"
status: done
priority: P2
type: security
estimate: small
chunk: null
blocked_by: []
plan: null
decision: null
created: 2026-04-20
assignee: agent
artifacts: ["docs/team-artifacts/security/2026-04-13-T-13-preflight-security.md", "docs/team-artifacts/qa/2026-04-14-T-13-preflight-validation.md"]

Carry-over from the T-13 security audit: `formation_audit_log` INSERT on warning-override does not bind `user_id` to `auth.uid()` from a server-trusted source. A user with a stale or substituted JWT could log overrides under another identity. Tighten the insert path so `user_id` is always set from the SvelteKit `event.locals.user.id` (or equivalent) and never from client-supplied input.

**Orchestrator hint**: `type: security` + `estimate: small` → recipe `0 → 4 → 5(security-analyst) → 7b`. No brainstorm — the fix is mechanical. The security-analyst owns the post-fix verification.

## acceptance

- Every `formation_audit_log` insert (documents action, suivi quest action, any other call sites) reads `user_id` only from server-side authenticated context
- No code path accepts `user_id` from `FormData` or query params
- security-analyst sign-off recorded in the ticket log

## log

- 2026-04-21 implementer: SQL migration 20260421100000 binds formation_audit_log_insert policy to auth.uid(); audit-log.ts now exports AuthenticatedUserId branded type via authenticatedUserId() helper; all 35 existing call sites updated to wrap user.id at the boundary (apprenants ×3, formateurs ×6, programme ×10, seances ×6, finances ×4, documents ×5 incl. checkedUser, suivi ×1); TransitionContext.userId + cancelFormationDocuments param in document-lifecycle.ts updated; test helpers updated; bun run check clean (72 errors, same as pre-existing baseline, no new errors); supabase db reset applied successfully.
- 2026-04-21 security-analyst: PASS — migration drops then recreates formation_audit_log_insert policy with `user_id = auth.uid() AND EXISTS(...)` WITH CHECK (INSERT-only, role authenticated, no SELECT/UPDATE/DELETE touched); AuthenticatedUserId brand opaque (`string & { readonly __brand }`), JSDoc forbids client input; all 34 prod call sites (3 test fixtures excluded) source user.id from `locals.safeGetSession()` or its `checkedUser` alias; negative grep for formData/params/url returned zero hits; coverage check confirms every file calling logAuditEvent/logFieldUpdate uses the brand directly or receives it via typed param.