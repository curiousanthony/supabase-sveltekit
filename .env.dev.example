# Template pour le développement local. Copier vers .env et remplir les valeurs.
# Après avoir lancé "supabase start", récupérer les clés avec : supabase status

# Sign in with GitHub
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Sign in with Google
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=""
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=""

AUTH_REDIRECT_URI="http://127.0.0.1:54321/auth/v1/callback"

# Supabase local (Docker). DATABASE_URL : après "supabase start". anon key / service_role via "supabase status"
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
PUBLIC_SUPABASE_URL="http://127.0.0.1:54321"
PUBLIC_SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
PROD_SUPABASE_PROJECT_PASS=""

# Optional: Slack webhook for daily recap (Cursor /daily-recap). Create an Incoming Webhook in your Slack channel.
# SLACK_WEBHOOK_URL="https://hooks.slack.com/services/..."